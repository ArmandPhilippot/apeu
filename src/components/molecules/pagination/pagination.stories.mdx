---
title: "Pagination"
---
import { components as mdxComponents } from "../../mdx";
import CodePreview from "../code-preview/code-preview.astro";
import Pagination from "./pagination.astro";

export const components = mdxComponents;
export const renderLink = (page) => `#page-${page}`;

## Introduction

A `Pagination` component should be used when a collection of items is divided in multiple pages.

<CodePreview
  code={`import Pagination from "./components/organisms/pagination/pagination.astro";`}
  label="How to import"
  lang="ts"
/>

<CodePreview
  code={`<Pagination current={1} last={4} renderLink={renderLink} />`}
  label="Basic use"
  lang="astro"
>
  <Pagination current={1} last={4} renderLink={renderLink} />
</CodePreview>

## With ellipsis and current page is first page

<CodePreview
  code={`<Pagination current={1} last={10} renderLink={renderLink} />`}
  label="Showing ellipsis when current page is the first one"
  lang="astro"
>
  <Pagination current={1} last={10} renderLink={renderLink} />
</CodePreview>

## With ellipsis and current page is last page

<CodePreview
  code={`<Pagination current={10} last={10} renderLink={renderLink} />`}
  label="Showing ellipsis when current page is the last one"
  lang="astro"
>
  <Pagination current={10} last={10} renderLink={renderLink} />
</CodePreview>

## With both ellipsis and current page is in the middle

<CodePreview
  code={`<Pagination current={25} last={50} renderLink={renderLink} />`}
  label="Showing ellipsis on both side when current page is in the middle"
  lang="astro"
>
  <Pagination current={25} last={50} renderLink={renderLink} />
</CodePreview>

## With custom siblings count

<CodePreview
  code={`<Pagination
    current={25}
    last={50}
    renderLink={renderLink}
    siblings={3}
/>`}
  label="Using a custom siblings count"
  lang="astro"
>
  <Pagination
    current={25}
    last={50}
    renderLink={renderLink}
    siblings={3}
  />
</CodePreview>
