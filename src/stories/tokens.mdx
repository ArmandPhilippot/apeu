---
title: "Tokens"
---
import TokenPreview from "../components/stories/token-preview.astro";
import { components as mdxComponents } from "../components/mdx";

export const components = mdxComponents;

{/* See `src/styles/tokens/borders.css` for the available tokens. */}
export const borders = {
  radius: ["sm", "md", "lg", "pill", "rounded"],
  sizes: ["sm", "md", "lg"],
};

{/* See `src/styles/tokens/colors.css` for the available tokens. */}
export const surfaces = {
  names: [
    "regular",
    "muted",
    "primary",
    "critical",
    "discovery",
    "idea",
    "info",
    "success",
    "warning",
  ],
  variants: [
    "low",
    "lighter",
    "light",
    "default",
    "dark",
    "darker",
    "high",
  ],
};

{/* See `src/styles/tokens/fonts.css` for the available tokens. */}
export const fonts = {
  families: ["mono", "regular"],
  sizes: ["sm", "md", "lg", "xl", "2xl", "3xl", "4xl", "5xl"],
  weights: ["bold", "regular"],
};

{/* See `src/styles/tokens/shadows.css` for the available tokens. */}
export const shadows = {
  elevations: ["raised", "elevated", "floating"],
  directions: {
    x: ["right", "center", "left"],
    y: ["bottom", "top"],
  },
};

{/* See `src/styles/tokens/spacings.css` for the available tokens. */}
export const spacings = [
  "4xs",
  "3xs",
  "2xs",
  "xs",
  "sm",
  "md",
  "lg",
  "xl",
  "2xl",
];

## Fonts

### Family

<div class="story-wrapper story-flex">
  {
  fonts.families.map((family) => (
    <TokenPreview isInline token={`--font-family-${family}`}>
      <p style={`font-family: var(--font-family-${family});`}>
        The quick brown fox jumps over the lazy dog
      </p>
    </TokenPreview>
  ))
  }
</div>

### Size

<div class="story-wrapper story-flex">
  {
  fonts.sizes.map((size) => (
    <TokenPreview isInline token={`--font-size-${size}`}>
      <p style={`font-size: var(--font-size-${size});`}>
        The quick brown fox jumps over the lazy dog
      </p>
    </TokenPreview>
  ))
  }
</div>

### Weights

<div class="story-wrapper story-flex">
  {
  fonts.weights.map((weight) => (
    <TokenPreview isInline token={`--font-weight-${weight}`}>
      <p style={`font-weight: var(--font-weight-${weight});`}>
        The quick brown fox jumps over the lazy dog
      </p>
    </TokenPreview>
  ))
  }
</div>

## Spacings

<div class="story-wrapper story-flex">
  {
  spacings.map((spacing) => (
    <TokenPreview isInline token={`--spacing-${spacing}`}>
      <div
        style={`display:flex;flex-flow:row nowrap;gap: var(--spacing-${spacing});`}
      >
        <div class="story-token-box" style="border-color: var(--color-border);" />
        <div class="story-token-box" style="border-color: var(--color-border);" />
      </div>
    </TokenPreview>
  ))
  }
</div>

## Borders

### Size

<div class="story-wrapper story-flex">
  {
  borders.sizes.map((size) => (
    <TokenPreview isInline token={`--border-size-${size}`}>
      <div
        class="story-token-box"
        style={`border-color: var(--color-border);border-width: var(--border-size-${size});`}
      />
    </TokenPreview>
  ))
  }
</div>

### Radius

<div class="story-wrapper story-flex">
  {
  borders.radius.map((radii) => (
    <TokenPreview isInline token={`--border-radii-${radii}`}>
      <div
        class="story-token-box"
        style={`border-color: var(--color-border);border-radius: var(--border-radii-${radii});`}
      />
    </TokenPreview>
  ))
  }
</div>

## Surfaces

<div class="story-wrapper story-grid">
  {
  surfaces.names.map((context) => (
    <div class="story-wrapper story-flex">
      {surfaces.variants.map((variant) => {
        const token = variant === "default"
          ? `--color-${context}`
          : `--color-${context}-${variant}`;
        return (
          <TokenPreview token={token}>
            <div class="story-token-box" style={`background: var(${token});`} />
          </TokenPreview>
        );
      })}
    </div>
  ))
  }
</div>

### Texts on surfaces

<div class="story-wrapper story-grid">
  {
  surfaces.names.map((context) => (
    <div class="story-wrapper story-flex">
      <TokenPreview token={`--color-on-${context}`}>
        <div
          class="story-token-box"
          style={`background: var(--color-${context});color: var(--color-on-${context});`}
        >
          Preview
        </div>
      </TokenPreview>
    </div>
  ))
  }
</div>

## Shadows

### Elevation

<div class="story-wrapper story-flex">
  {
  shadows.elevations.map((elevation) => (
    <TokenPreview isInline token={`--shadow-${elevation}-to-top-left`}>
      <div
        class="story-token-box"
        style={`background: var(--color-regular);padding: var(--spacing-md);`}
      >
        <div
          class="story-token-box"
          style={`background: var(--color-regular);border-color: var(--color-border);box-shadow: var(--shadow-${elevation}-to-top-left);`}
        />
      </div>
    </TokenPreview>
  ))
  }
</div>

### Direction

{
  shadows.directions.y.map((y) => (
  <div class="story-wrapper story-grid">
    {shadows.directions.x.map((x) => (
      <TokenPreview token={`--shadow-elevated-to-${y}-${x}`}>
        <div class="story-token-box" style={`padding: var(--spacing-md);`}>
          <div
            class="story-token-box"
            style={`--shadow-color: var(--color-black);background: var(--color-regular);border-color: var(--color-border);box-shadow: var(--shadow-elevated-to-${y}-${x});`}
          />
        </div>
      </TokenPreview>
    ))}
  </div>
  ))
}

### Colors

Ideally, I should adapt the shadow color according to the surface but I don't think I'll use them elsewhere than on regular background.

### Inner shadows

Inner shadows can be used to create a relief effect. Two tokens are available to match the surface colors.

<TokenPreview isInline token={`--color-shadow-inset`}>
<div
  class="story-token-box"
  style={`background: var(--color-regular-light);padding: var(--spacing-md);`}
>
  <div
    class="story-token-box"
    style={`background: var(--color-regular);box-shadow: 0 0 0 2px var(--color-shadow-inset);`}
  />
</div>
</TokenPreview>

<TokenPreview isInline token={`--color-shadow-inset-dark`}>
<div
  class="story-token-box"
  style={`background: var(--color-regular);padding: var(--spacing-md);`}
>
  <div
    class="story-token-box"
    style={`background: var(--color-regular-dark);box-shadow: 0 0 0 2px var(--color-shadow-inset-dark);`}
  />
</div>
</TokenPreview>
