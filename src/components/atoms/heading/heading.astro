---
import type { HTMLTag, Polymorphic } from "astro/types";

type Props<
  T extends Extract<HTMLTag, "h1" | "h2" | "h3" | "h4" | "h5" | "h6"> = "h2",
> = Polymorphic<{ as: T }>;

const { as: Tag = "h2", ...attrs } = Astro.props;
---

<Tag {...attrs}><slot /></Tag>
