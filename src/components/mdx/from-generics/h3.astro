---
import type { ComponentProps } from "astro/types";
import Heading from "../../atoms/heading/heading.astro";

type Props = ComponentProps<typeof Heading<"h3">>;

const { as = "h3", ...props } = Astro.props;
---

<Heading {...props} as={as}><slot /></Heading>
