---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"samp">;

const { class: className, ...attrs } = Astro.props;
---

<samp {...attrs} class:list={["samp", className]}><slot /></samp>

<style>
  .samp {
    font-family: var(--font-family-mono);
  }
</style>
