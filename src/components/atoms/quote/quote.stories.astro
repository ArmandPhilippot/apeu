---
import type { ComponentProps } from "astro/types";
import PageLayout from "../../templates/page-layout/page-layout.astro";
import Heading from "../heading/heading.astro";
import QuoteComponent from "./quote.astro";

const title = "Quote";
const breadcrumb: ComponentProps<typeof PageLayout>["breadcrumb"] = [
  { label: "Home", url: "/" },
  { label: "Design system", url: "/design-system" },
  { label: "Components", url: "/design-system/components" },
  { label: "Atoms", url: "/design-system/components/atoms" },
  { label: title, url: Astro.url.href },
];
const seo: ComponentProps<typeof PageLayout>["seo"] = {
  nofollow: true,
  noindex: true,
  title: breadcrumb
    .slice(1)
    .reverse()
    .map((crumb) => crumb.label)
    .join(" | "),
};
---

<PageLayout breadcrumb={breadcrumb} seo={seo} title={title}>
  <Fragment slot="body">
    <Heading as="h2">Usage</Heading>
    <p>This component is used to display an inline quote in your contents.</p>
    <Heading as="h2">In English</Heading>
    <p lang="en">
      This is a quote <QuoteComponent>Lorem ipsum</QuoteComponent> in an english
      text.
    </p>
    <Heading as="h2">In French</Heading>
    <p lang="fr">
      {/* cSpell:disable */}
      Ceci est une citation <QuoteComponent>Lorem ipsum</QuoteComponent> dans un
      texte français.
      {/* cSpell:enable */}
    </p>
  </Fragment>
</PageLayout>
