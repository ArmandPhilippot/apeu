---
title: "ContactView"
isStandalone: true
---
import { components as mdxComponents } from "../../components/mdx";
import ContactView from "./contact-view.astro";

export const components = mdxComponents;
export const title = "ContactView";
export const breadcrumb = [
  { label: "Home", path: "/" },
  { label: "Design system", path: "/design-system" },
  { label: "Views", path: "/design-system/views" },
  { label: title, path: "/design-system/views/contact-view" },
];
export const seo = {
  nofollow: true,
  noindex: true,
  title: breadcrumb
    .slice(1)
    .reverse()
    .map((crumb) => crumb.label)
    .join(" | "),
};
export const fakePage = {
  Content: null,
  collection: "pages",
  description: "The contact page.",
  hasContent: false,
  headings: [],
  id: "en/contact",
  locale: "en",
  meta: {
    publishedOn: new Date(),
    updatedOn: new Date(),
  },
  route: "/contact",
  seo: {
    description: "",
    title,
  },
  slug: "contact",
  title,
};

<ContactView breadcrumb={breadcrumb} entry={fakePage} seo={seo} />
