---
import type { ComponentProps } from "astro/types";
import List from "../../atoms/list/list.astro";

type Props = ComponentProps<typeof List<"ol">>;

const { as = "ol", ...props } = Astro.props;
---

<List {...props} as={as}><slot /></List>
