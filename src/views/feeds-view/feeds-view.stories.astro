---
import type { ComponentProps } from "astro/types";
import PageLayout from "../../components/templates/page-layout/page-layout.astro";
import FeedsViewComponent from "./feeds-view.astro";

const title = "FeedsView";

const breadcrumb: ComponentProps<typeof PageLayout>["breadcrumb"] = [
  { label: "Home", url: "/" },
  { label: "Design system", url: "/design-system" },
  { label: "Views", url: "/design-system/views" },
  { label: title, url: Astro.url.href },
];

const seo: ComponentProps<typeof PageLayout>["seo"] = {
  nofollow: true,
  noindex: true,
  title: breadcrumb
    .slice(1)
    .reverse()
    .map((crumb) => crumb.label)
    .join(" | "),
};

const fakePage: ComponentProps<typeof FeedsViewComponent>["entry"] = {
  Content: Fragment,
  collection: "pages",
  description: "The feeds page.",
  hasContent: false,
  headings: [],
  id: "en/feeds",
  locale: "en",
  meta: {
    publishedOn: new Date(),
    updatedOn: new Date(),
  },
  route: "/feeds",
  seo: {
    description: "",
    title,
  },
  slug: "feeds",
  title,
};
---

<FeedsViewComponent breadcrumb={breadcrumb} entry={fakePage} seo={seo} />
