---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"address">;

const { class: className, ...attrs } = Astro.props;
---

<address {...attrs} class:list={["address", className]}><slot /></address>
