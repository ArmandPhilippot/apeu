---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"figcaption">;

const { class: className, ...attrs } = Astro.props;
---

<figcaption {...attrs} class:list={["figcaption", className]}>
  <slot />
</figcaption>
