---
"apeu": minor
---

Adds support for setting card sizes at content level.

Previously, the minimal card size could have two fixed value: either `30em` or `22em`. Now, the size can be defined per content in the frontmatter.
