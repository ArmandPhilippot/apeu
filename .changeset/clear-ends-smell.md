---
"apeu": patch
---

Uses Bunny as font provider instead of Fontsource.

Satori doesn't support `.woff2` fonts and Fontsource doesn't seem to support Inter in `.woff` format. To fix this issue, I swap the font provider to Bunny.
